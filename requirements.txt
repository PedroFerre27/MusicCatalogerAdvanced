# Music Cataloger Client — dipendenze runtime
# Generate: pip install -r requirements.txt

# GUI
customtkinter>=5.2.0
Pillow>=10.0.0

# Audio metadata
eyeD3>=0.9.7
mutagen>=1.47.0

# HTTP client + server communication (v1082 pilot)
requests>=2.31.0

# Opzionali (solo per BPM offline)
# librosa>=0.10.0
# numpy>=1.24.0
